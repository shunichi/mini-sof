%h1 Mini Stackoverflow

#questions
  - @questions.each do |question|
    .question-summary
      .stats-container
        .stats
          .vote-stats
            %span.vote-count>= question.vote_sum
            %span.vote-unit 票
          .answer-stats{class: answer_stats_class(question) }
            %span.answer-count>= question.answers.size
            %span.answer-unit 答
      .summary
        %h3= link_to question.title, question
        .excerpt= truncate(question.body, length: 100)
        .user-info= question.user.email
